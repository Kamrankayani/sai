---
- name: Install Cilium CNI 
  hosts: control_plane
  become: true
  
  tasks:
    - name: Download Cilium CLI
      get_url:
        url: https://github.com/cilium/cilium-cli/releases/latest/download/cilium-linux-amd64.tar.gz
        dest: /tmp/cilium.tar.gz
        mode: '0644'

    - name: Extract Cilium CLI
      unarchive:
        src: /tmp/cilium.tar.gz
        dest: /tmp
        remote_src: yes

    - name: Install Cilium binary
      copy:
        src: /tmp/cilium
        dest: /usr/local/bin/cilium
        mode: '0755'
        remote_src: yes

    - name: Clean up temp files
      file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/cilium.tar.gz
        - /tmp/cilium

    - name: Uninstall any existing Cilium installation
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        cilium uninstall --wait
      ignore_errors: true

    - name: Wait for cleanup
      wait_for:
        timeout: 10

    # NEW: Get control plane IP for Cilium configuration
    - name: Get control plane private IP
      set_fact:
        control_plane_ip: "{{ hostvars['control-plane']['ansible_default_ipv4']['address'] }}"

    - name: Display control plane IP
      debug:
        msg: "Control plane IP for Cilium: {{ control_plane_ip }}"

    - name: Wait for all nodes to be visible
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        while [ $(kubectl get nodes --no-headers 2>/dev/null | wc -l) -lt 2 ]; do
          echo "Waiting for nodes..."
          sleep 5
        done
        echo "All nodes are visible"

    # FIXED: Complete Cilium installation with k8sServiceHost
    - name: Install Cilium with COMPLETE configuration
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        cilium install \
          --set kubeProxyReplacement=true \
          --set ipam.mode=kubernetes \
          --set routingMode=native \
          --set autoDirectNodeRoutes=true \
          --set ipv4NativeRoutingCIDR=10.0.0.0/16 \
          --set bpf.masquerade=true \
          --set endpointRoutes.enabled=true \
          --set k8sServiceHost={{ control_plane_ip }} \
          --set k8sServicePort=6443
      register: cilium_install

    - name: Display installation output
      debug:
        var: cilium_install.stdout_lines
      when: cilium_install.stdout_lines is defined

    - name: Wait for Cilium to be ready
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        cilium status --wait
      register: cilium_status
      changed_when: false
      retries: 30
      delay: 10
      until: cilium_status.rc == 0

    # NEW: Validate DNS is working
    - name: Validate DNS resolution
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        kubectl run dns-test --image=busybox:1.28 --rm -i --restart=Never -- nslookup kubernetes.default
      register: dns_test
      until: dns_test.rc == 0
      retries: 12
      delay: 10
      ignore_errors: true

    - name: Show cluster nodes
      become_user: ubuntu
      shell: |
        export KUBECONFIG=/home/ubuntu/.kube/config
        kubectl get nodes -o wide
      register: nodes_status

    - name: Display nodes
      debug:
        var: nodes_status.stdout_lines